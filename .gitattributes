# Generated by `make third-party`, and 89 of its lines come from upstream
# licence texts written with CRLF. Left byte for byte as produced, because git
# normalising them would make `make third-party-check` fail on a fresh clone for
# a reason that has nothing to do with the dependencies.
THIRD-PARTY.md -text
